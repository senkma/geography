# Materiály v aplikaci

Soubory z `content/inbox/` se po zpracování kopírují sem.

URL v datech: `./materials/{field}/{course}/{soubor.pdf}`

Nepřidávej sem soubory ručně — použij inbox, ať je evidence v `_inbox.json`.
